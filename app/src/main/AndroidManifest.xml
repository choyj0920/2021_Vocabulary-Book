<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.VocabularyBook">
    <!-- 갤러리 권한 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 갤러리 권한 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".AddwordActivity"
            android:theme="@style/MyTheme.noTitle.Dialog" />
        <activity android:name=".WordfindTextActivity" />
        <activity android:name=".WordfindImgActivity" />
        <activity
            android:name=".WordfindSelectActivity"
            android:theme="@style/MyTheme.noTitle.Dialog" />
        <activity
            android:name=".SelectWordbookActivity"
            android:theme="@style/MyTheme.noTitle.Dialog" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MywordbooklistActivity" />
        <activity android:name=".WordbookActivity" />
        <activity android:name=".WordlistActivity" />
        <activity android:name=".WordmemoryActivity" />
        <activity android:name=".WordtestActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>